export default function PageLoader() {
  return (
    <div className="app-page flex items-center justify-center" role="status" aria-live="polite">
      <p className="text-lg sm:text-xl text-[var(--text-muted)]">Cargando…</p>
    </div>
  );
}
